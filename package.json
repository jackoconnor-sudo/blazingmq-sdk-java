{
  "name": "bloomberg-cypress-e2e",
  "version": "1.0.0",
  "description": "End-to-end Cypress tests for bloomberg.com",
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run --browser electron --headless",
    "cy:run:chrome": "cypress run --browser chrome"
  },
  "devDependencies": {
    "cypress": "13.13.0"
  }
}
